The extension registers exactly one context menu item, "Manage keyboard shortcuts", scoped to its own toolbar action (contexts: ["action"]). Clicking it opens chrome://extensions/shortcuts. No page, selection, or link contexts are used. Tab moving, pinning, and closing use chrome.tabs methods that require no host or "tabs" permission. The only other permission requested is tabGroups, which reports whether a neighboring tab group is collapsed.
